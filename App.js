import React from 'react';
import {AppRegistry} from 'react-native';
import Root from './src/root';

const App = () => {
  return <Root />;
};

AppRegistry.registerComponent('DualApps', () => App);

export default App;
